# The TL3238x controller library used by this build does not provide the
# low-power symbols required by the generic Telink lock application.
CONFIG_PM=n

# Preserve the MTD role used by the sleepy lock configuration without PM.
CONFIG_CHIP_THREAD_DEVICE_ROLE_END_DEVICE=y

# TL3238x resource profile. Keep the standard lock data model while sizing
# protocol pools for the available SRAM.
CONFIG_CHIP_PERSISTENT_SUBSCRIPTIONS=n
CONFIG_CHIP_MAX_FABRICS=2

CONFIG_TL_BLE_CTRL_THREAD_STACK_SIZE=1536
CONFIG_MAIN_STACK_SIZE=3560
CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=1024
CONFIG_OPENTHREAD_THREAD_STACK_SIZE=2048

CONFIG_OPENTHREAD_NUM_MESSAGE_BUFFERS=32
CONFIG_NET_PKT_RX_COUNT=4
CONFIG_NET_PKT_TX_COUNT=4
CONFIG_NET_BUF_RX_COUNT=8
CONFIG_NET_BUF_TX_COUNT=12

CONFIG_NVS_LOOKUP_CACHE_SIZE=128
CONFIG_SETTINGS_NVS_NAME_CACHE_SIZE=64
CONFIG_SETTINGS_NVS_SECTOR_COUNT=2
